services:
  - type: web
    name: tv-webhook
    env: python
    plan: free
    autoDeploy: true
    startCommand: "gunicorn app:app --workers=2 --threads=4 --timeout=120"
    envVars:
      - key: WEBHOOK_SECRET
        sync: false   # 在 Render 控制台手动填写，避免泄露到仓库
